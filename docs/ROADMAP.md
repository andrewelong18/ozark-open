# Roadmap & Sprint Tracker

The phased build plan **and** the live status tracker. Phases describe *what* gets built; sprints are the numbered, sitting-sized units of work we execute one at a time. This file is the dashboard — the single source of truth for "where are we?" **The work for each sprint lives in its own file under [`sprints/`](sprints/)** — start there, not here.

> **How to start a sprint:** open `docs/sprints/sprint-N.md` (self-contained: goal, reads, blockers, checkboxes, done-when). Check the blocker sprint's "Done when" if it's a dependency. Come back here only to flip status when the sprint ships.

> **Re-planned July 15, 2026** around Pat & Jake's new betting architecture (`docs/adr/0001-bet-pick-architecture.md`): bets now have picks, betting windows are phases, results arrive per pick from the admin's spreadsheet, and the menu is published via upload. The old Sprints 1–6 were renumbered and re-cut; two new sprints (schema rework, ingestion) were inserted up front. The July 11 audit's **Spike A** (bet-taxonomy design meeting) is resolved — the memo *is* its outcome — and **Spike B** (void → pool math) was confirmed July 15 (PRD §12 A7). Remaining open calls live in `OUTSTANDING_DECISIONS.md`.

---

## Timeline

- **Tournament:** September 24–26, 2026
- **Fully wrapped:** September 10, 2026 at the latest (two weeks before tee-off)
- **Feature freeze:** ~August 28, 2026 — after this, only bugs, polish, and dry-run fixes
- Coding is Claude-Code-assisted, so build time per sprint is short. The pacing constraints are **deploy verification and human testing** — not code. The schedule absorbed two extra sprints without moving the freeze; it's tighter than before, so don't let sprints sprawl.

| Window | Work |
|---|---|
| Week of Jul 13 | Sprint 0 (deploy & verify) |
| Week of Jul 20 | Sprints 1–2 (bet/pick schema rework, spreadsheet ingestion) |
| Late Jul – early Aug | Sprints 3–5 (bet placement, the heart of the app) |
| Mid August | Sprints 6–7 (results display, payouts) |
| Late August | Sprint 8 (leaderboard) → **feature freeze Aug 28** |
| Sept 1–10 | Sprint 9 (mobile pass, group dry run, fixes) |
| Sept 24–26 | 🏌️ Ozark Open |

> **The freeze was reopened once, deliberately** (Andrew, Sept 7, 2026). A second dry run with
> Pat produced four changes that are worth more than the freeze protects: the bet menu's toggle
> is wrong for how the weekend actually runs, the undo button can't be reached from a phone, and
> on Saturday night the app's front door still shows a *Place Bets* button. They are cut as
> **Sprints 26–28**, targeted Sept 12 / 15 / 20, and each is independently revertable. Recorded
> here rather than left as drift: the Aug 28 line held for nine days of real work, and the next
> person reading this file should see that it was reopened on purpose and by whom.

---

## Sprint Index

One sprint = one sitting. Open the linked file to work it; don't start a sprint while its blockers are open. **Legend:** ✅ complete · 🔶 in progress · 🔲 not started · ⚠️ needs rework

> **What ✅ means here** (Andrew, Aug 9, 2026): **the code shipped.** A row is ✅ once its
> "Done when" holds in the repo — verification still owed in a production browser is named as
> **residue** inside the cell, by issue number, and never used as the reason to hold a row at
> 🔶. 🔶 is reserved for sprints where something genuinely wasn't built or
> genuinely hasn't happened. This convention was applied across the
> whole file in an audit on Aug 9, 2026, which also found Sprint 17 recorded as not started
> three weeks after it shipped.
>
> **And what a cut item inside a ✅ row means** (Andrew, Sept 7, 2026): a sprint can ship with one
> of its own checkboxes deliberately abandoned — Sprint 13's landing-page ad slot, Sprint 9's group
> dry run, Sprint 9's aggregate-stats stretch. Those are struck through in the sprint file with a
> date and a reason, and the row still reads ✅ because everything that was going to be built was.
> Sprint 9 was this file's standing example of 🔶 until Sept 7, when the group run was cut rather
> than run; ❌ stays for a sprint cut in its entirety, as Sprint 14 was.

| Sprint | Focus | Status | Blockers |
|---|---|---|---|
| [0](sprints/sprint-0.md) | Deploy & verify foundations | ✅ "Done when" met Jul 16, 2026; the hardening items closed over the following week (Resend #16, JWT #17, admins #15, display names #6/#11). Open residue, neither of which is code: ~~the Supabase Pro decision (#18)~~ **decided Aug 9, 2026 — pay for Pro for September, upgrading late August so it's one billed month; the steps are in `PRE_TOURNAMENT_CHECKLIST.md` §Late August** and Jake's admin flag (#60, waits on him signing in once) | none |
| [1](sprints/sprint-1.md) | Bet/pick schema rework & menu rebuild | ✅ merged Jul 17, 2026 (PR #10); prod SQL applied + confirmed Jul 18, 2026 (#12 — 13 bets/57 picks; render check #13/#8 closed the same day). The Jul 31 dry run then drove this menu for real | 0 |
| [2](sprints/sprint-2.md) | Spreadsheet ingestion (`/admin/import`) | ✅ code complete + local round-trip Jul 17, 2026; prod SQL applied Jul 18, 2026 (#12); prod import verified (#19, closed). Pat uploaded four sheets through it in the Jul 31 dry run, and `e2e/` now round-trips it on every run | 1 |
| [3](sprints/sprint-3.md) | Placements schema & validation | ✅ validation tests pass + prod migration applied Jul 18, 2026 (#22 — RLS on, 5 policies) | 1 (after 2) |
| [4](sprints/sprint-4.md) | Placement API & place-bet UI | ✅ code complete + tested locally Jul 17, 2026 (unit + local-Postgres RLS); prod browser flow verified (#24, closed Jul 25) and now driven end to end by `e2e/placement.spec.ts` on every run | 3 |
| [5](sprints/sprint-5.md) | My Bets & phase compliance | ✅ code complete + tested locally Jul 17, 2026; prod verify closed (#26). The compliance copy changed again in Sprint 22 — that copy's browser check is #119 | 4 |
| [6](sprints/sprint-6.md) | Results & closed-bet views | ✅ code complete + tested locally Jul 18, 2026; prod SQL applied Jul 18, 2026 (#28). **Residue:** browser check #31 and Pat's unaided walkthrough #30 — the latter largely overtaken by the Jul 31 dry run. Sprint 19's `e2e/results-and-reveal.spec.ts` covers the reveal, and caught it being dead under PGRST201 for two sprints | 2 + 4 |
| [7](sprints/sprint-7.md) | Payouts: theoretical & final | ✅ code complete + tested locally Jul 18, 2026 (unit tests incl. the 2026 worked example, PG16 round trip for the view); view migration + full chain applied Jul 18, 2026 (#34). The Jul 31 dry run reconciled the real pool to the cent. **Residue:** Pat's hand-math cross-check #37, the `tournament.status` flip at tournament end #36, browser pass #35 | 6 |
| [8](sprints/sprint-8.md) | Leaderboard mirror | ✅ code complete + unit-tested + built locally Jul 21, 2026 (`lib/leaderboard.ts` reads the "Sportsbook Leaderboard" tab read-only, 5-min cache; `/leaderboard` 8-column table with `+0;-0;"E"` to-par formatting; nav tab + route guard). **Residue is entirely outside the repo:** the Google Cloud service account + Vercel env vars (#66), which gate the "within 5 min" check (#68), the mobile restack check (#138), and Pat keeping the tab populated (#67) | Pat's Sheets mirror |
| [9](sprints/sprint-9.md) | Polish & group dry run | ✅ **Closed Sept 7, 2026 — with the group run cut rather than run**, and the sprint file says so in those words: its "Done when" (*"the dry run completes without an admin needing to touch code"*) was **not met**, because Andrew pasted a patched seed script on the September night ([#189](https://github.com/andrewelong18/ozark-open/issues/189)). Closed by decision, on substitute evidence, three days from the wrap date. **The dry run itself ran twice against production** — Jul 31, 2026 with Pat (full lifecycle; pool reconciled to the cent at entry fees $425 − voided stakes $32 = **pool $393**, all 87 picks settled, zero pending; 21 findings → Sprints [21](sprints/sprint-21.md)–[25](sprints/sprint-25.md) and issues #90–#109; record in [`docs/dry-run/ISSUE_LOG.md`](dry-run/ISSUE_LOG.md)) and again in **September 2026**, which produced four asks → Sprints [26](sprints/sprint-26.md)–[28](sprints/sprint-28.md) (#193–#199) plus #189. **Polish half shipped Aug 9, 2026:** the mobile pass (safe-area handling — `viewport-fit: cover` was never set, so every `env(safe-area-inset-*)` silently resolved to 0; 44px targets on every control on the betting path; the leaderboard and results tables stacked instead of parked behind horizontal scrollers), proven by a **Pixel 7 Playwright project** rather than by eye — 24 specs, tap targets measured by where taps actually LAND, one wager placed end to end with `tap()`; before/after screenshots in [`docs/mobile/`](mobile). Plus [`PRE_TOURNAMENT_CHECKLIST.md`](PRE_TOURNAMENT_CHECKLIST.md) and [`scripts/db-export.sh`](../scripts/db-export.sh) + [`DATA_SAFETY.md`](DATA_SAFETY.md). **What was never tested, and is named rather than absorbed:** ~5 real strangers on their own phones at once — first-time onboarding, concurrent load at a deadline, and the Resend delivery leg, exercised exactly once ever. That moved into the checklist as a soft-open step the week of, the iOS pass as a named day-before step, and a written answer for "members can't sign in". **Residue:** #137 (prod export run), #139 (real iOS device), #189 (seed-script bug), and the four questions still open on #111. The aggregate-stats stretch was cut the same day | 0–7 (8 nice-to-have) |
| [10](sprints/sprint-10.md) | ★ Admin roster & registration status | ✅ code complete Jul 25, 2026 (`tournament_invites` + `admin_auth_activity()` RPC, `lib/roster.ts` status derivation + relative-time util — unit-tested + built locally); **prod SQL applied + verified Jul 25, 2026**. **Page superseded by Sprint 20** (Jul 26): `/admin/roster` redirects to `/admin/people`; the schema and `lib/roster.ts` carry forward unchanged, and `e2e/admin-approval.spec.ts` drives the console that replaced it. **Residue:** none — #80 asked for a walkthrough of a page that no longer exists and was closed as obsolete on Aug 9, 2026; the console that replaced it is covered by [#125](https://github.com/andrewelong18/ozark-open/issues/125) | none hard (bonus) |
| [11](sprints/sprint-11.md) | ★ Bet-state snapshots & rollback | ✅ Code complete + fully verified locally Aug 9, 2026 (`npm test` 351 · `local-db-verify` incl. a new 19-check snapshot round trip · `sim-pool-verify` · `dry-run-verify` end to end at $425 − $32 = $393 · `test:e2e` 37/3). The tournament can now undo its worst five minutes: every import snapshots the money tables before it applies and **refuses to run if the snapshot fails**, there's a Snapshot-now button for hand edits, and `scripts/restore-snapshot.ts <id> --yes` rolls all five tables back in one transaction. Two things the plan didn't anticipate: `TRUNCATE` would have cascaded away the hand-typed invite roster (`tournament_invites` is not in the payload), and the round-trip's invite check was **vacuous** until it seeded an invite of its own — the seed has none, so it compared 0 to 0. Scheduling moved from Vercel Cron to Supabase pg_cron to keep a service-role key out of the app. **Scheduling live in prod Aug 9, 2026:** `pg_cron` enabled and `ozark-snapshot` scheduled at `0 */6 * * *` through the Management API — #141 was filed as a dashboard step and turned out to be plain SQL. Proven by firing a throwaway every-minute job (4 `succeeded` runs, 4 `trigger='cron'` snapshots) rather than by reading `cron.job`. **Residue:** none — #142 applied, #141 closed | 3 in prod (bonus) |
| [12](sprints/sprint-12.md) | ★ Animation & delight pass + Jake celebration | ✅ **complete Aug 23, 2026** — motion half Aug 12, celebration half Aug 23. Motion half: (6 commits, zero new dependencies; 389 unit tests · lint · tsc · build · **e2e 49 passed / 0 failed**). The vocabulary lives in the design skill first — `tokens/motion.css` + two playable `guidelines/motion-*.card.html` specimens — so a new page inherits motion the way it inherits colour; `DESIGN_SYSTEM.md` § 4 is the app-side port. Every curve is a named Material 3 or Penner value, framed on IBM Carbon's productive/expressive split. Applied to route changes, list entrances, the bet-menu filter swap, the toast's enter *and* exit, press/hover across 16 sites, and `loading.tsx` skeletons. Fixed two pre-existing bugs on the way in: the dialog had not animated since Sprint 18 (`duration-[--dur-slow]` is Tailwind v3 syntax that v4 compiles to `0s`), and `results-and-reveal.spec.ts` had been failing since Aug 9 unnoticed. Three techniques were tried, measured and reverted — `0fr→1fr` collapses, React `<ViewTransition>`, and gating `hover:` — each written up so they don't get re-attempted. **Celebration half, Aug 23, 2026 (#164 closed):** the blocker cleared in an unexpected shape — one 1.63s video with the audio burned in, not the specced photo + sound clip — so it ships as a video micro-interaction. De-letterboxed off a measured `cropdetect` (`crop=676:720:302:0`, all 47 frames) and normalised from a hot −10.9 LUFS to −16; MP4 only because VP9/WebM measured *larger* on a clip this short (255KB vs 214KB). Three non-obvious constraints, all documented: the `<video>` never unmounts (an autoplay unlock belongs to the element, so a remount throws it away — which is also why the entrance replays via forced reflow, not a React `key`), playback is armed inside the click rather than after the `await` (the gesture has expired by the time the API answers, and iOS Safari refuses an audible `play()` outright), and nothing takes a pointer event (a centre-screen overlay would fail Playwright's actionability check in four existing specs). First call site of `--ease-overshoot`, unused since the vocabulary shipped. Two departures from the sprint text, recorded rather than silently diverged: **no mute toggle** (Andrew's call — a muted *fallback* remains for browsers that refuse audible autoplay, but it is not a user control), and it fires on edited stakes as well as new wagers. Reduced motion needs no branch — but measured under the floor, `animationend` does **not** fire, so the 600ms exit fallback is the only thing returning the card to rest. Verified in a real browser against the compiled stylesheet and the real asset. **Residue:** `e2e/celebration.spec.ts` is written but unexecuted — no Docker on the build machine (#172); `hover:` gating (#165); a real-phone browser pass (#166). | — |
| [13](sprints/sprint-13.md) | ★ Funny ad slots (dashboard; landing cut) | ✅ **Dashboard half shipped Aug 24, 2026** — recorded here Aug 31, 2026 after an audit found this row still reading 🔲 a week after the creatives went live, the same drift the Aug 9 audit caught on Sprint 17. Five creatives in `public/ads/` with `lib/ads.ts` as the manifest and `components/ads/ad-carousel.tsx` as the slot, in the dashboard's right rail under the activity feed. It **rotates** on a 5s timer rather than picking one per load, pausing on hover and on keyboard focus (WCAG 2.2.2), 4:3 rather than the specced 3.2:1, and productive-tier motion because a thing that loops forever in the corner of a page must not read as a nag. The **landing-page slot is cut** (Andrew, Aug 31, 2026): past the freeze, and the gag belongs inside the app rather than on the page people see before they are members. The sprint doc's spec drift is corrected in place ([#171](https://github.com/andrewelong18/ozark-open/issues/171)). **Residue:** no creative carries an `href` yet ([#170](https://github.com/andrewelong18/ozark-open/issues/170)) — the URLs are Andrew's to supply | ≥1 finished creative from Andrew — **delivered Aug 24, 2026** (bonus) |
| [14](sprints/sprint-14.md) | ★ Announcement banner (admin-toggled, dismissible) | ❌ **Cut Aug 31, 2026** (Andrew). Never started, and it is past the Aug 28 feature freeze with the group dry run still unrun — the remaining time is worth more spent proving what exists than adding a surface nobody asked for twice. Nothing depends on it | — (cut) |
| [15](sprints/sprint-15.md) | ★ Self-serve profile page (nickname, avatar, status; Competitive Analysis §1.1) | ✅ code complete Jul 19, 2026 (nickname + avatar_url columns, `avatars` storage bucket, `/profile`, admin entry relocated here, name+nickname everywhere — unit-tested + built locally); prod SQL applied (#49) and browser-verified (#50), both closed. **Residue:** avatar *upload* (the Sprint 21 RLS P0 #90) shipped Aug 8, 2026 and is covered by `e2e/avatar-upload.spec.ts`; the prod browser check is #145 | none (bonus) |
| [16](sprints/sprint-16.md) | ★ First-run onboarding, self-set identity & admin bettor-approval (Competitive Analysis §1.2, expanded) | ✅ code complete Jul 20, 2026 (`onboarded_at` + relaxed guard, `/onboarding` flow + walkthrough, middleware gate, `/admin/participants` approval — unit-tested + built locally); prod SQL + flow verified (#54, #58, both closed), and `e2e/onboarding.spec.ts` walks it on every run. **Page superseded by Sprint 20** (Jul 26): `/admin/participants` redirects to `/admin/people`; its API is unchanged | none hard (bonus); builds on 1 + 4 + 15 |
| [17](sprints/sprint-17.md) | ★ Bet-slip review & confirmation (Competitive Analysis §1.3 + §1.5) | ✅ shipped Jul 18, 2026 — `components/betting/bet-slip-summary.tsx` is the sticky balance bar, `bet-placement-card.tsx` the two-tap confirm strips and the one-line "✓ Locked in $5 at +110" receipt (reworded and de-duplicated Aug 31, 2026), and `/bets` reuses `normalizeMyBets` + `checkTournamentTotal` + `buildComplianceSummary` verbatim so it can't disagree with `/my-bets`. Covered by `e2e/placement.spec.ts` + the Pixel 7 project. **Status flipped Aug 9, 2026** — an audit found this row still reading 🔲 while all five checkboxes had been live for three weeks | none hard (bonus); builds on 4 + 5 |
| [18](sprints/sprint-18.md) | ★ Player profile modals (clickable names → animated profile + 4-year chart) | ✅ code complete Jul 23, 2026 (6 admin-owned profile columns seeded dummy, first modal primitive on Base UI, `PlayerChip` on `/bets` + `/results` — unit-tested + built locally); prod migration applied + modal verified (#73, closed). **Residue:** ~~real per-member copy replacing the dummy seed (#74)~~ **done Sept 9, 2026 — see the profile/roster row below**, and leaderboard names — deliberately cut, not forgotten (#75) | none hard (bonus); builds on 15 |
| [19](sprints/sprint-19.md) | Automated testing & pre-prime-time pool simulation (Playwright E2E · DB round-trips in CI · full-pool synthetic seed) | ✅ Code complete + fully verified locally Aug 9, 2026 (`npm test` 345 · `local-db-verify` · `dry-run-verify` end to end, pool unchanged at $393 with 56 picks linked · **`test:e2e` 13 specs green** · **`test:sim` 32 members / 256 wagers / $926 pool reconciled to the cent**). One command now walks the whole member journey in a real browser — sign-up → onboarding → approval → place/edit/remove → reveal-at-close → payouts — and the DB round-trips plus the pool simulation gate every PR. **It paid for itself on the first run:** Sprint 23's `placed_by_user_id` FK had made the `users` embed ambiguous, so `/bets` showed "No wagers on this bet" on every closed bet and `/admin/view` showed nothing — the reveal had been dead since Sprint 23 and no unit test or SQL round-trip could see it. Three local-stack setup faults and two doc drifts fixed alongside | none hard; app code-complete through 8 |
| [20](sprints/sprint-20.md) | Admin people console (roster + participants, merged) | ✅ shipped Jul 26, 2026 — `/admin/people` is the whole access funnel with approve/edit/revoke attached per row; `/admin/roster` + `/admin/participants` now redirect to it and the admin panel is down to three buttons. `buildRoster` is the single derivation (the onboarded-only filter that hid stalled members is gone), and #82's bulk invite paste box shipped with it. Open decision resolved **(a) access control only** (Pat, Jul 26) — no audit trail, no migration. The walkthrough #86 closed Aug 9 when `e2e/admin-approval.spec.ts` automated it, right through to the approved member placing a wager against the fee the admin typed. **Residue:** the invite box and name editing are still uncovered (#135) | none hard; builds on 10 + 16 |
| [21](sprints/sprint-21.md) | Dry-run bug fixes (avatar RLS · revoke destroys the entry fee · silent $0 rejection · tooling) | ✅ cut Jul 31, 2026; **four of five items shipped Aug 7, 2026** — the money P0 (#91) is fixed as a **soft revoke** (`revoked_at`; eligibility = row exists AND not revoked, PRD §12 **A13**; fee and wagers now leave and return together), the silent $0 rejection (#92), both tooling bugs (#94, #95 — the CTE collision was in four files, not two) and the gameplan errata (#109) are done. Unit-tested (195 pass), `local-db-verify.sh` + `dry-run-verify.sh` both green, same reconciliation as Jul 31 ($425 − $32 = $393). The prod migration #113 has since been applied and closed. **All five shipped as of Aug 9, 2026** — the last one was the avatar RLS P0 (#90), which landed Aug 8 (`lib/avatar.ts`) and had its diagnosis corrected Aug 9: the "race against cookie hydration" it was fixed for **does not exist**, because supabase-js awaits `getSession()` inside its own fetch wrapper on every storage request. The four bucket policies and the bucket config were read directly from production and are correct — they were never the fault. `e2e/avatar-upload.spec.ts` now proves the upload end to end through real RLS, both at onboarding and on `/profile` (the UPDATE path), which nothing had ever covered. #90 closed, #93 unblocked. **Residue:** the avatar prod browser check (#145 — unusually load-bearing, since the original failure was never captured and can't be reproduced), the revoke browser round trip (#114), one live magic-link run (#115), and the bucket's missing server-side size/MIME limits (#144) | none |
| [22](sprints/sprint-22.md) | Rule reinforcement & import guards (tournament-wide pick minimum · status/result validation · chase-query rewrite) | ✅ Code complete + fully verified locally Aug 8, 2026 (#96–#98 all four items: `min_picks_per_tournament` migration, `checkPickMinimum()`, both import guards, phase-aware chase query with a one-line "text these people" answer; PRD §12 A14/A15 and ADR 0001 §10 amended; `OUTSTANDING_DECISIONS` §2 closed). Prod migration **applied and verified Aug 8, 2026** ([#117](https://github.com/andrewelong18/ozark-open/issues/117), closed) | none hard; land 21 first |
| [23](sprints/sprint-23.md) | Admin control surface (editable display names · house-rules editor · add a member and bet on their behalf) | ✅ Code complete + fully verified locally Aug 8, 2026 (#99, #100, and the wager half of #101); **prod migration applied and verified the same day**. ADR 0001 §13 was settled first: bettor and actor are separate, only the bettor is the subject of §7, and the member's own RLS policy was never loosened — a separate admin-scoped pair carries `placed_by_user_id = auth.uid()`, so the database refuses a forged attribution rather than trusting the route. `/bets?for=<member>` is the real menu with the member's budget and limits. **The account half of #101 shipped Aug 9, 2026** ([#124](https://github.com/andrewelong18/ozark-open/issues/124)): the service-role route, decided over three rejected alternatives in ADR 0001 §14 because it is the only one where the member can still claim the account later. It brings this project's first full-bypass key into the runtime, confined to one module with one importer and asserted by a test. Building it surfaced that there was **no admin `UPDATE` policy on `public.users`** — so this sprint's own #99 name edit had been a silent no-op for everyone but the acting admin, invisible to every local harness because they all run as superuser | none hard; builds on 20 |
| [24](sprints/sprint-24.md) | Betting menu UX (handicap badges · collapsible reveal · filter refactor · pick ordering) | ✅ Code complete + fully verified locally Aug 8, 2026 (#102–#105). No schema, as planned. Two things the plan didn't anticipate: **#105 was a no-op on `main`** — `sortPicks` shipped in `524ad92` and `/bets` called it, but `bets-menu.tsx` re-sorted by `sheet_pick_id` at both render sites and threw the ordering away, so the feature never reached the screen; and the Sprint 23 on-behalf thread had **no coverage**, so a filter refactor that dropped the prop would have recorded an admin's wager for a member against the admin. The prop is now required (a `tsc` error to drop) and the endpoint swap is unit-tested. The stroke strip is one helper shared with the importer, with a test pinning display name === match key — divergence there silently unlinks a pick and disables its §7 self-bet cap, self-pick flag and opponent block. **Follow-up Aug 12, 2026** from Pat's phone pass (#127): a card read the tournament-wide placements map as its own (**#161**), so one wager made every other pick-one bet refuse selection *and* left that wager with no remove control — placed, uneditable, unremovable, phase wide open; and the pick-one radio is gone (**#162**) in favour of a stake box on every row, with the siblings disabled once a wager exists | none |
| [25](sprints/sprint-25.md) | Phase scheduling, close automation & countdown | ✅ Code complete + fully verified locally Aug 8, 2026 (#106–#108). ADR 0001 §5a settled first: the scheduler never writes `bets.status` — a deadline per phase on the `tournaments` row gates wagering alongside it, so the upload only opens a bet and the clock only closes a phase. No cron, no job runner. `/admin/close` is the September-readiness P0 answered — chase list, clock controls and a guarded final unlock, all off the database. Prod migration **applied and verified Aug 8, 2026** ([#117](https://github.com/andrewelong18/ozark-open/issues/117), closed) | 22 (the import guards interact) |
| — | ★ Dashboard activity feed (`COMPETITIVE_ANALYSIS.md` §2.4) | ✅ Code complete Aug 31, 2026 — the dashboard's Activity rail, empty since Sprint 5, is now a live chat-shaped timeline: a linked name + "placed a bet" per wager, phase openings and closings, and 19 hand-written house lines rolled in deterministically — those name members and render through the same row component as a wager, indistinguishable by design (Andrew's call, PRD §12 A16); they are fiction and touch no data. Three things were settled before building it. **The privacy line** (PRD §12 A16): named, never positioned — the pick and amount stay behind the close, enforced by `activity_placements()`'s five-column result and asserted by `scripts/activity-rls-roundtrip.ts` on every `local-db-verify` run, not by the UI. **"Real time"** is a 20s poll plus a focus refetch (§2.5's middle ground), not Realtime — PRD §3's ban is on a tote board, not on fresh data. **The quips are deterministic**, seeded off the preceding event's id: `Math.random()` would have re-dealt the feed on every poll and re-animated the screen under the reader. One migration (`bets.opened_at` + the function), one importer stamp on the `→ open` transition only. Verified: 425 unit tests, lint, tsc, build, and `local-db-verify` end to end including the new round-trip. **Residue:** `e2e/activity-feed.spec.ts` is written but unexecuted — no Docker on this machine (the #172 constraint). **Extended Sept 7, 2026:** a fourth event kind — *"<member> joined the sportsbook"* when someone signs up and finishes onboarding, linked like every other row (PRD §12 A16). No migration: `users.onboarded_at` is the stamp and `public.users` is already authenticated-read-all, so the arrivals ride the roster read the house lines use, capped like the placement window and degrading with it. 478 unit tests, lint, tsc, build green; the new e2e case shares the #172 constraint | none |
| — | Launch readiness: entry collection · the over-commit guard · a health check · the completion blind spot | ✅ Code complete Sept 2, 2026, from a holistic pre-launch review rather than the roadmap. Four gaps a real launch exposes, each independently revertable. **(A)** `tournament_participants` carried the entry fee and nothing about whether the money arrived, so the app could answer "what do I get" to the cent and not "who still owes" at all — three columns now record it on `/admin/people` (PRD §12 **A17**), never deciding the mechanism (`OUTSTANDING_DECISIONS.md` §3 stays open) and never an input to pool math. **(B)** PRD §7 rule 6 was a TypeScript promise: read, sum, write, no lock — two concurrent wagers on different picks both landed, $30 on a $20 entry. Now a trigger that locks the bettor's row `FOR UPDATE` (**A18**), and the concurrency test was **proven able to fail** by deleting the lock and watching it go red. **(C)** `/api/health` runs the reads the app depends on, because on Aug 31 a deploy outran its migration and Vercel and Supabase both stayed green through the outage. **(D)** `buildComplianceSummary()` returned nothing at zero placements, so the member who had done the least was the only one told nothing — the dry run has two of them on the record (§2b: Steve paid, never wagered, finished at −$20.00). Verified: 471 unit tests, lint, tsc, build, `local-db-verify` end to end with a new collection round trip. **Residue:** both migrations must be applied to prod **before** merging; `e2e/` still unexecuted (#172) | none |
| [26](sprints/sprint-26.md) | Phase-first bet menu (Phase 1 \| Phase 2 toggle · per-phase status badge) | ✅ Code complete + fully verified locally **Sept 7, 2026** (`npm test` 474 · `tsc` · lint · build · `dry-run-verify.sh` end to end, pool unchanged at $425 − $32 = **$393** with the payout table matching Appendix A row for row — the check that proves a presentation sprint stayed one). Cut from the **second dry run with Pat**. `/bets` now toggles Phase 1 \| Phase 2, both tabs always rendering, and an unpublished Phase 2 says so instead of vanishing — Pat's sentence, more or less verbatim. **Two Sprint 24 decisions reversed knowingly** (PRD §12 **A19**): the axis, and "sorting bets … stays". Paid for without a second control — wagerable bets sort first, every card is badged (open cards carried none), and the badge moved off the `<h1>`, where it described the whole book and read "Open" mid-tournament over a closed phase, onto the toggle it now describes. #104's real prize survives one level down: a phase tab may be empty by design, but every chip is derived from the selected phase. Rounds and categories merged into one scrolling chip row, spelled out — three rows of chrome became two. **The finding that would have made the sprint a no-op:** `bet-filters.test.ts`'s fixture had phase and status perfectly correlated, with 12 of 18 tests on it, so a rewrite would have gone green whether or not the axis moved; every fixture now mixes statuses inside one phase and the suite was **proven able to fail** by sabotage (3 red). Fixed [#195](https://github.com/andrewelong18/ozark-open/issues/195) by construction on the way past, and gave the E2E seed its first Phase 2 (two `hidden` bets, opened by re-upload in the spec that needs them). [#193](https://github.com/andrewelong18/ozark-open/issues/193), [#194](https://github.com/andrewelong18/ozark-open/issues/194), [#195](https://github.com/andrewelong18/ozark-open/issues/195) closed. **Residue:** the phone browser pass ([#201](https://github.com/andrewelong18/ozark-open/issues/201)) and `test:e2e` unexecuted for want of Docker (#172), so this sprint's three new specs have never run. Filed not fixed: `seed-sim-pool.sql` carries the same phase↔status correlation the unit fixture did ([#202](https://github.com/andrewelong18/ozark-open/issues/202)). **Follow-up Sept 10, 2026 — the merged chip row is reversed (PRD §12 A23).** Pat drove the menu a third time and asked for **three** filter levels that compose: phase, then round, then category, each a radio group. Rounds and categories are separate rows again and genuinely independent — `Facet` became `BetFilter { phase, round, category }` and `filterPhases()` applies two predicates, so "Phase 1 + Round 1 + Match" is representable for the first time. The options went **static** (`ROUNDS_BY_PHASE`, `CATEGORIES` — a new `lib/bet-taxonomy.ts` that also kills four duplicated copies of these lists), which **gives up #104's guarantee that no selectable option can empty the page**; the fixed, non-reshuffling rows and a real, E2E-asserted "no bets match" state naming both active chips are what pay for it. **"Medalist is not a bet category" was a real hole:** `bet_categories` has no CHECK constraint and `validateSheet()` took its legal names from every row of that table, so one stray row made an off-contract category importable and then a filter chip — the five are pinned in code now and the parameter is gone. **`test:e2e` finally ran** (Docker available): 73 pass / 8 fail, and **all 8 reproduce identically on `main`** from a freshly-reset database, so they are this sprint's own never-executed specs (#201), not regressions | none |
| [27](sprints/sprint-27.md) | Admin save-state console (`/admin/snapshots` — list + guarded restore) | ✅ Code complete + fully verified locally **Sept 8, 2026** (`npm test` 485 · `tsc` · lint · build · `local-db-verify.sh` end to end including a new **30-check** round trip · `dry-run-verify.sh` end to end, 69 checks, pool unchanged at $425 − $32 = **$393** with 0 pending). Cut Sept 7, 2026 from the second dry run. **Explicitly reverses Sprint 11's "KEEP IT BORING … No restore UI"**, which was right when restore was a developer's tool and is wrong now: Sprint 23 spent a whole sprint on the premise that *Pat can run the tournament without database access*, and this is the last hole in it — the tool for the single most likely weekend disaster (uploading last week's sheet, fat-fingering a cell) is the one he cannot reach. One migration adding two `SECURITY DEFINER` functions: `snapshot_index()` (an RPC rather than a PostgREST select, because `jsonb_array_length` can't be expressed in a `select` and `payload` is hundreds of KB per row — the listing must never select it) and `restore_snapshot()`, a faithful port of `scripts/restore-snapshot.ts`'s transaction, `tournament_invites` stash and all. **Restore takes a `pre-restore` snapshot first and requires a typed confirmation** (Andrew, Sept 7) — the undo button gets its own undo. `scripts/restore-snapshot.ts` stays: it is the path that works when the app is down. **The reversal is recorded in four places, not one** — PRD §12 **A21** (which was already written at scoping time, read true, and gained a clause naming the limit that bounds it), the older migration's own header comment, `DATA_SAFETY.md` (rewritten so the console is the primary undo path and the script is what works when the app is down), and `CLAUDE.md`'s admin-surface line. **Three decisions taken during the build, all departures from the issue as written and all argued in the sprint file:** the `tournament_invites` stash is `jsonb` rather than a temp table (under `search_path = ''` a temp table needs `pg_temp` qualification and collides with plpgsql's plan cache — and the jsonb form names *no* column, where the temp table still needs a `WHERE`); the orphan-account pre-check is derived from `pg_constraint` rather than from a list of columns, which removed the "names three columns" cost the plan had accepted; and the pre-restore snapshot passes a **NULL** keep, since `take_snapshot()` already treats that as "prune nothing" — making the retention hazard structurally impossible rather than merely unlikely. **Proven able to fail**, which is the part that mattered: four sabotages, each run and reverted — removing the invite stash turns 2 checks red, removing the `ozark.restoring` stand-down kills the restore on `OZ001`, a keep of 1 evicts the save state being restored, and inverting `describeDelta`'s sign turns 2 unit tests red. Also fixed the drift the sprint created but didn't name: `README.md`'s "rolling back is one command", `DATA_MODEL.md`'s note that only the script sets `ozark.restoring`, and the import report's "Undo this import" block, which printed a CLI command needing a laptop to an admin on a phone. [#196](https://github.com/andrewelong18/ozark-open/issues/196) closed. **Residue:** the phone-browser pass ([#204](https://github.com/andrewelong18/ozark-open/issues/204)). Filed not fixed: `scripts/policy-manifest.ts` covers `pg_policy` only and has **no drift check on function `EXECUTE` grants at all** ([#205](https://github.com/andrewelong18/ozark-open/issues/205)). **DEFECT, Sept 11, 2026 — the button was dead in prod from the day it shipped**, and the prod migration *was* applied (the failure proves it). `restore_snapshot()` cleared the five tables with unqualified `DELETE`s, and Supabase preloads `pg-safeupdate` on the `authenticator` role PostgREST connects as, which refuses `WHERE`-less DML inside a `SECURITY DEFINER` plpgsql body. Fixed by `20260911000000` (six `WHERE true`). **The 30-check round trip could not have caught it**: it reaches the RPC over `psql` as the database owner and imitates the role with `SET ROLE`, but `session_preload_libraries` resolves at connect time for the *login* role. That blindness is now asserted (hazard 8, against the installed `prosrc`), and `PRE_TOURNAMENT_CHECKLIST.md` gained a restore rehearsal — the root cause under the SQL is that nobody had pressed the button in production | none |
| [28](sprints/sprint-28.md) | The closing leaderboard (dashboard → final standings · sortable columns · post from the final import) | ✅ **Code complete Sept 8, 2026** (502 unit tests · lint · `tsc --noEmit` · `next build` · `local-db-verify.sh` · `dry-run-verify.sh` end to end, pool unchanged at **$425 − $32 = $393**, 0 pending, 56 picks linked). Cut Sept 7, 2026 from the second dry run, with Pat's written column schema as source of truth. **Writes no new payout math** — `buildResultsTable()` already computes his exact formula and returns his exact six fields, and `/results` already renders them; the work is sorting, placement and the trigger. Two decisions taken up front (Andrew, Sept 7): **one table, one truth** — the dashboard becomes the settlement surface and `/results` retires to a redirect, because two tables ranking the same 32 people by different columns is a thing to argue about at 11pm; and **the import offers the post, a human taps it** — publishing everyone's payouts as a side effect of a file upload would remove the one moment somebody reads the numbers first, which is the entire reason `finalizeReadiness()` exists (#108). Scoping found the load-bearing bug: **five reads filter tournaments to `upcoming, active`**, so a finalized tournament would have rendered "No active tournament" on the dashboard and "No bets published yet" on `/bets`. Items: [#197](https://github.com/andrewelong18/ozark-open/issues/197), [#198](https://github.com/andrewelong18/ozark-open/issues/198), [#199](https://github.com/andrewelong18/ozark-open/issues/199). Owes PRD §12 **A20** and updates to the checklist, README and `CLAUDE.md`. **The safest of the three to slip** — the fallback is the button that already exists. **As built:** `lib/standings.ts` holds the ordering and no money, and its suite was proven able to fail — seven sabotages, each confirmed red. One of them caught a vacuous test on the way past: the tie-break case tied on a column where `buildResultsTable()` already emits rows alphabetically, so a comparator with *no* tie-break passed on a stable sort. The equivalent gap existed in `settlement.test.ts`, whose order assertion used equal entry fees and no voids and therefore could not tell payout order from P/L order at all. Both fixtures were rebuilt to discriminate. The settlement text now sorts by P/L too — `lib/settlement.ts` had a standing "do not re-sort, the text and the page must agree" rule written when both lived on `/results`; obeying it meant changing the order once the text was pasted from beneath a P/L-ranked table. Phones got a sort control the sprint didn't specify, because the column headers are `sm:`+ only and the feature would otherwise not have existed on the device this page is read on — and the mobile E2E label assertions had to be scoped to the rows, since those new chips carry the same five words and would have satisfied them with the row labels gone. **Not verified:** the three retargeted E2E specs are unexecuted for want of Docker (#172) | none hard; land 26 + 27 first |
| [29](sprints/sprint-29.md) | The upload deletes what its sheet dropped (phase-scoped sweep · two-pass confirmation · the wagered tier) | ✅ **Code complete Sept 12, 2026** (539 unit tests · lint · `tsc --noEmit` · `next build` · `local-db-verify.sh` incl. a 14-check sweep round trip · `dry-run-verify.sh` end to end, pool unchanged at **$425 − $32 = $393**). Cut Sept 12, 2026 from Pat, the day after a Phase 1 refresh left 19 stale bets live on the menu: *"anytime I upload a sheet it should delete all bets that aren't in the sheet being uploaded. It can warn me that I am about to delete some bets."* `buildImportPlan()` had only ever added, so a bet that dropped out of the spreadsheet stayed on the menu forever with no path in the app to remove it. **Four calls, all in PRD §12 A24:** scope is the phases the sheet mentions and not the whole tournament (narrower than Pat's literal words, so a phase-2 staging sheet on a phone at 10pm cannot offer to delete Phase 1); picks are swept as well as whole bets; rows carrying wagers are kept by default and cleared only on a separate control, with their bettors named; and clearing **hard-deletes** the placements, **knowingly reversing PRD §8.2 and §10** on the grounds that the `pre-import` snapshot carries `bet_placements` in full — proven, not asserted, by a round-trip check that sweeps a wagered bet and restores it. The delete is one `SECURITY DEFINER` function rather than a policy plus three PostgREST calls, because PostgREST cannot span calls and the half that lands first is the money, and because granting admins a blanket `DELETE` on `bet_placements` to serve one importer path is far wider than the job. **A typed confirmation was recommended for the wagered tier and overruled** (Pat chose two taps); the mitigation is that the tiers never share a control. **Residue:** the prod migration (#229), the phone-browser pass on the confirm panel (#230), and the E2E spec for the two-pass handshake (#231) | none hard |

> **★ Sprints 10–18 are the bonus wish list** (Andrew, Jul 18, 2026): enhancements, never MVP blockers. Work them only when no MVP sprint (0–9) is waiting on you; they must land before the Aug 28 feature freeze or be cut without ceremony — the tournament runs fine without them. *(Sprint 15 — the Competitive Analysis §1.1 profile page — was cut into a file Jul 19, 2026. Sprint 16 — the §1.2 first-run onboarding stub, expanded on Andrew's Jul 20 direction to also cover self-set identity + admin bettor-approval — was cut into a file Jul 20, 2026. Sprint 17 keeps its `COMPETITIVE_ANALYSIS.md` §6 number. Sprint 18 — clickable player profile modals — was added Jul 23, 2026 on Andrew's direction.)*

---

## Status Summary (by phase)

| Phase | What | Status | Sprint(s) | Target |
|---|---|---|---|---|
| 0 — Foundations | Skeleton, deploy pipeline | ✅ **Verified in production Jul 16, 2026** | 0 | Jul 18 |
| 1 — Auth | Magic-link login, users table | ✅ **Verified in production Jul 16, 2026** (magic-link login → dashboard) | 0 | Jul 18 |
| 2 — Tournament setup | Tournaments, participants, dashboard | ✅ **Verified in production Jul 16, 2026** | 0 | Jul 18 |
| 3 — Bet menu | Bets **and picks**, `/bets` page | ✅ Reworked to ADR 0001, merged Jul 17, 2026; prod SQL applied and confirmed Jul 18, 2026 (#12 — 13 bets/57 picks; #13/#8 closed). Driven for real by Pat on Jul 31 | 1 | Jul 24 |
| 3b — Spreadsheet ingestion | `/admin/import` upload pipeline | ✅ Code complete + local round-trip Jul 17, 2026; prod SQL applied Jul 18, 2026 (#12); prod import verified (#19). Four sheets went through it in the Jul 31 dry run, and `e2e/` round-trips it on every run | 2 | Jul 31 |
| 4 — Placing bets | Placements, validation, My Bets | ✅ **Sprints 3–5 all code complete Jul 17, 2026** (schema, validation, placement API, `/bets` UI, `/my-bets`, compliance banners, dashboard rework, admin chase SQL); prod browser verification closed (#24, #26), and since Sprint 19 the whole path — place, edit, remove, revive — runs in a real browser on every `test:e2e`. **Residue:** the Sprint 22 pick-count copy is unverified in a browser (#119) | 3, 4, 5 | Aug 12 |
| 5 — Results | Closed-bet views, per-pick results via re-upload | ✅ Code complete + tested locally Jul 18, 2026 (everyone's placements on closed bets, derived settled badge, users read-all migration, README runbook); prod SQL applied Jul 18, 2026 (#28), and `e2e/results-and-reveal.spec.ts` now asserts the reveal on rendered DOM — the test that caught it dead under PGRST201. **Residue:** browser pass #31, Pat's unaided walkthrough #30 | 6 | Aug 16 |
| 6 — Theoretical payouts | Payout view, per-placement display | ✅ Code complete + tested locally Jul 18, 2026 (`placement_payouts_view` proven on local PG16, My Bets per-pick payouts + rollup, `/admin/view` View-sheet replica); prod SQL applied Jul 18, 2026 (#34). The Jul 31 dry run read these numbers against the real board. **Residue:** browser pass #35 | 7 | Aug 21 |
| 7 — Final payouts | Pari-mutuel split (void-adjusted pool), `/results` | ✅ Code complete + tested locally Jul 18, 2026 (`lib/payouts.ts` split, `/results` gated on `completed`, 2026 worked example + void case unit-tested); view + chain applied Jul 18, 2026 (#34), and the Jul 31 dry run reconciled the live pool to the cent ($425 − $32 = $393). **Residue:** Pat's hand-math cross-check #37 and the `tournament.status` flip at tournament end #36 — the flip is now a guarded control on `/admin/close` rather than a Studio edit | 7 | Aug 21 |
| 8 — Leaderboard | Google Sheets mirror | ✅ Code complete + tested locally Jul 21, 2026 (`lib/leaderboard.ts` read-only Sheets fetch of the "Sportsbook Leaderboard" tab, `parseLeaderboard`/`formatToPar` unit-tested, 5-min `unstable_cache`; `/leaderboard` 8-column table, always-visible nav tab, middleware guard). **Everything left is outside the repo:** the Google Cloud service account + Vercel env vars (#66), which gate the "within 5 min" check (#68) and the mobile restack check (#138); Pat keeps the tab populated (#67) | 8 | Aug 28 |
| 9 — Polish & dry run | Mobile pass, group test | ✅ **Closed Sept 7, 2026.** The polish half completed Aug 9, 2026 — mobile pass on every page (proven by a Pixel 7 Playwright project: 24 specs, no route overflows 412px, every control on the betting path a real 44px target, one wager placed with `tap()`), the pre-tournament checklist, and the DB export script + runbook. The **group half was cut rather than run** — the row is ✅ with that item struck through in [`sprints/sprint-9.md`](sprints/sprint-9.md), not ticked as if it happened. What closed it: two full-lifecycle sessions with Pat against production (Jul 31 and September), 17 Playwright specs including the §7 rules gauntlet through real RLS, a 32-member synthetic pool reconciling to within $0.02, and `dry-run-verify.sh`'s 64 checks. What it does **not** cover — ~5 strangers on their own phones at once, and the Resend delivery leg — became operational steps in the pre-tournament checklist instead of an open checkbox | 9 | Sep 10 |
| Testing & simulation | E2E browser journeys, DB round-trips in CI, full-pool synthetic seed | ✅ Code complete + fully verified locally Aug 9, 2026. The money-critical paths ~32 people will walk have now been walked automatically: 13 Playwright specs across 7 journeys, real magic-link sign-in through the real callback (no test-only auth bypass), and every assertion on rendered DOM rather than HTTP 200 — the discipline #105 taught. RLS, import idempotency, the payout view and a 32-person pool reconciliation run on every PR; the browser suite stays off the merge gate by design. Sprint 9's dry run is now a content check, not a plumbing check | 19 | before Aug 28 |
| Admin consolidation | One people console — the access funnel with approve/edit/revoke attached | ✅ Shipped Jul 26, 2026 (`/admin/people`; the two old routes redirect, net −1 admin page, bulk invite entry folded in) | 20 | before Aug 28 |
| Tournament clock & settle | Phase deadlines, countdown, `/admin/close` | ✅ Code complete + fully verified locally Aug 8, 2026 (`npm test` 246 · `local-db-verify` · `dry-run-verify` end to end). The tournament's clock and its two time-critical controls now live in the app instead of the SQL editor, and the final unlock cannot silently publish an inflated split — the dry run proves the hazard on the real dataset ($628.67 → $525.77 denominator against an unchanged $393 pool) | 25 | before Aug 28 |
| Admin control surface | Editable display names, house-rules editor, betting on a member's behalf | ✅ Code complete + fully verified locally Aug 8, 2026 (`npm test` 298 · `local-db-verify` · `dry-run-verify` end to end, pool unchanged at $393); prod migration applied and verified. The three walls Pat hit in the dry run are gone — correcting a name, changing a rule and entering someone else's wager no longer need database access. The money table gained an audit column and an admin-scoped policy pair, and the two placement routes now share one write path so the bettor's identity can't quietly become the admin's. Account creation without an email round-trip **completed the surface on Aug 9, 2026** (#124) — an admin creates a real, confirmed account from `/admin/people` with no email sent, and can bet for that member immediately. Pat can now run the tournament without database access end to end | 23 | before Aug 28 |
| Betting menu UX | Handicap badges, collapsible reveal, filter refactor, pick ordering | ✅ Code complete + fully verified locally Aug 8, 2026 (`npm test` 337 · `local-db-verify` · `dry-run-verify` end to end, pool unchanged at $393, 56 picks still linked). The page ~32 people refresh all weekend now reads on a phone: a closed bet shows totals with the names collapsed behind an "x bettors" toggle instead of a wall, one filter is active at a time with the open/closed view computed from what's actually on the page (Phase 1 closed while Phase 2 is open — both exist at once), and handicaps are badges beside the name rather than glued into it. Two latent defects surfaced and were fixed: #105 had shipped as a no-op, and the Sprint 23 on-behalf hand-off was droppable without a compile error | 24 | before Aug 28 |
| Rule reinforcement | Tournament-wide pick minimum, import guards, phase-aware chase query | ✅ Code complete + fully verified locally Aug 8, 2026 (`npm test` 211 · `local-db-verify` · `dry-run-verify` end to end, pool table unchanged). The two rules the Jul 31 dry run changed are now what the code and the docs both say, and the importer refuses the results-on-an-open-book upload that started it. The additive migration is applied and verified in prod (#117) | 22 | before Aug 28 |
| Data safety — snapshots & rollback | Automatic save states of the money tables, and a one-command restore | ✅ Code complete + fully verified locally Aug 9, 2026 (`npm test` 351 · `local-db-verify` incl. a 19-check snapshot round trip · `sim-pool-verify` · `dry-run-verify` end to end, pool unchanged at $393 · `test:e2e` 37/3). Sprint 9's `db-export.sh` was the floor — the fire escape for "the database is gone". This is the undo button for the likely disaster instead: uploading last week's sheet, or fat-fingering a cell at 10pm. Every import now snapshots first and **refuses to run if the snapshot fails**, because a net you're told you have but don't is worse than none. The restore names no column anywhere — `to_jsonb` out, `jsonb_populate_recordset` back — so a future migration can't silently desync either end. Scheduling went to Supabase pg_cron rather than Vercel Cron, keeping a service-role key out of the app entirely — and as of Aug 9, 2026 it is **live in production**, verified by watching a temporary every-minute job actually fire rather than by confirming the row exists. **Sprint 27 completed it Sept 8, 2026** (`npm test` 485 · `local-db-verify` incl. a 30-check restore round trip · `dry-run-verify` 69 checks, pool unchanged at $393): the undo button is now reachable from a phone at `/admin/snapshots`, which **knowingly reverses** this migration's own "KEEP IT BORING … no restore UI" (PRD §12 **A21**, and the migration's comment is amended rather than left contradicting shipped behaviour). That line was right while restore was a developer's tool and stopped being right when Sprint 23 settled that Pat runs the tournament without database access. The restore is a `SECURITY DEFINER` **port** of the script's transaction, not a new design, and the script stays as the path that works when the app itself is down. Two things bound it: a `pre-restore` save state taken inside the same transaction, so the undo button has its own undo, and a typed confirmation over a panel naming what will be discarded — including that the `tournaments` row comes back too, so a restore rewinds the phase clock and can un-publish the results. **Residue:** the phone-browser pass (#204). **A defect closed the loop on Sept 11, 2026**: the restore button had never actually worked in production — `restore_snapshot()`'s unqualified `DELETE`s are refused by `pg-safeupdate`, which Supabase preloads on the role PostgREST connects as, and which no psql-driven harness in this repo can see. Fixed by `20260911000000`; the blindness itself is now asserted, and the checklist gained a restore rehearsal, because the deeper cause is that the button had never been pressed in prod | 11, 27 | before Aug 28 · 27 Sep 15 |
| Dry-run defect fixes | The five ways the app was wrong when Pat drove it | ✅ **all five shipped** — four on Aug 7, 2026 (soft revoke + pool arithmetic, stake-rejection messages, dry-run tooling, gameplan errata), and the revoke migration is applied in prod (#113, closed). the avatar-upload RLS P0 (#90) on Aug 8, with its diagnosis corrected Aug 9 — the policies were never wrong and the race it was fixed for isn't real; `e2e/avatar-upload.spec.ts` now covers both the onboarding and `/profile` paths through real RLS. #93 unblocked. **Residue:** avatar prod browser check #145, revoke round trip #114, live magic-link run #115, bucket limits #144 | 21 | before Aug 28 |
| Motion design system | A named, sourced motion vocabulary — in the design system first, the app second | ✅ Complete Aug 23, 2026. Motion half Aug 12, 2026 (389 unit tests · lint · `tsc --noEmit` · `next build` · **e2e 49 passed / 0 failed** against a real local stack). Zero new dependencies: CSS, `tw-animate-css` (installed since Sprint 18 and until now almost unused), and Base UI's transition attributes. The point of the sprint was that motion was the one foundation a new page *couldn't* inherit — colour, type, spacing and shadows all had token files and specimen cards; motion had one prose paragraph. It now has both, with every curve traceable to Material 3 or Penner rather than eyeballed, and IBM Carbon's productive/expressive split as the rule that decides which moments get which. Three techniques were built, measured and reverted rather than shipped on faith, each written up in `DESIGN_SYSTEM.md` § Motion. Every new test was verified by sabotage — restoring the bug it guards and confirming it fails. **The celebration half landed Aug 23, 2026 (#164 closed)** — a 1.63s video rather than the specced photo + sound clip, which turned it into a media-autoplay problem rather than an animation one: the unlock belongs to the element, so the `<video>` never unmounts and playback is armed inside the click rather than after the `await`. It is also the design system's one named exception to "no confetti, no attention loops", argued in `DESIGN_SYSTEM.md` § 4 rather than assumed — one-shot, user-initiated, and it carries information. Residue: the new spec is unexecuted for want of Docker (#172). | 12 | before Aug 28 |
| Launch readiness | Entry collection · the entry-fee cap in the database · `/api/health` · the zero-wager blind spot | ✅ Code complete Sept 2, 2026 (471 unit tests · lint · tsc · build · `local-db-verify` end to end incl. a new collection round trip). Not from the roadmap — from a holistic review three weeks out. Two migrations (`20260902000000`, `20260902000001`), **both of which must be applied to prod before the merge**; `/api/health` is the alarm if that order ever slips again. PRD §12 gained **A17** (collection is recorded, never adjudicated, and never a pool input) and **A18** (§7 rule 6 is a database guarantee — the lock, not the sum, is the fix). **Residue:** `e2e/` unexecuted for want of Docker (#172); an uptime monitor still has to be pointed at `https://ozark-open.com/api/health` (`PRE_TOURNAMENT_CHECKLIST.md` § Monitoring) | — | Sep 10 |
| Real player profiles & Roster | The roster's real copy, finishes as places, and `/roster` | ✅ Code complete Sept 9, 2026 (483 unit tests · lint · build · `local-db-verify` end to end incl. a new profile-seed round trip · browser pass at 1200px and 375px). Sprint 18 shipped the modal against placeholder copy and a fake chart; this is the real thing, plus the page that makes it browsable. The seed applies itself as the remaining 20 players register, which is the part that had to be a trigger rather than a script. One migration (`20260909000000`), **which must be applied to prod before the merge**. **Residue:** `e2e/` unexecuted (#172) | — | Sep 10 |
| ★ Bonus wish list | Admin roster · bet-state snapshots · animation & Jake celebration · funny ads · announcement banner · self-serve profile page · first-run onboarding + bettor-approval · bet-slip review & confirmation · player profile modals | 🔶 **Eight of nine shipped, one cut:** 10 (admin roster, absorbed into the Sprint 20 people console) ✅ · **11 (bet-state snapshots) ✅ Aug 9, 2026 — its own row below** · 15 (profile page) ✅ · 16 (onboarding + bettor-approval) ✅ · **17 (bet-slip review & confirmation) ✅** · 18 (player profile modals) ✅ · **12 (animation & Jake celebration) ✅ Aug 23, 2026 — its own row above** · **13 (funny ads) ✅ Aug 24, 2026 — dashboard slot live; landing slot cut Aug 31** · **14 (announcement banner) ❌ cut Aug 31, 2026** — all prod-applied and browser- or E2E-verified, including 11's migration (#142) and its pg_cron schedule (#141); avatar *upload* (#90) is fixed and E2E-covered too. **Not started:** 13 (funny ads), 14 (announcement banner) — 13 waits on assets from Andrew. None of these ever blocks MVP | 10–18 | as time allows, before Aug 28 |
| Post-dry-run: the bet menu | Phase-first toggle and a per-phase status badge on `/bets` | ✅ Code complete Sept 7, 2026 (474 unit tests · lint · tsc · build · `dry-run-verify.sh`, pool unchanged). The first phase-visible UI the betting menu has ever had — the tree carried `phase` since Sprint 1 and never rendered it. Reverses #104's status partition and its ordering rule, with three mitigations in place of a second control, and preserves the no-empty-page guarantee at the chip level where it can still hold. The unit suite was rebuilt around fixtures that mix statuses inside a phase, because the old one could not tell a phase axis from a status axis at all | 26 | Sep 12 |
| Post-dry-run: reachable rollback | `/admin/snapshots` — the save states, and a guarded restore button | ✅ Shipped Sept 8, 2026 (PR #206) — this row was left 🔲 when the sprint merged and is corrected here. Cut Sept 7, 2026. Reverses Sprint 11's "no restore UI" on the grounds that Sprint 23 already decided Pat runs the tournament without database access. The first destructive button in the app, so it carries a `pre-restore` snapshot and a typed confirmation | 27 | Sep 15 |
| Post-dry-run: the closing board | The dashboard becomes final standings; `/results` retires; post from the final import | ✅ Code complete Sept 8, 2026 (502 unit tests · lint · tsc · build · `local-db-verify` · `dry-run-verify` end to end, pool unchanged at $393). Cut Sept 7, 2026, to Pat's written column schema. No new payout math — the formula and the six columns already shipped, so the sprint was sorting, placement and the trigger. The last mile of the weekend moved to the app's front door: `tournaments.status = 'completed'` early-returns the standings from the dashboard, `/results` is a redirect (route kept — the README, the checklist and pasted links point at it), and four of five `["upcoming","active"]` tournament reads took `"completed"` so `/bets` and `/my-bets` don't go dark on the night people argue about a closed bet. The import *offers* the post and a human taps it, with an Unpost behind it — two taps, not Sprint 27's typed word, because nothing is destroyed. PRD §12 **A20**. **Residue:** the three retargeted E2E specs are unexecuted for want of Docker (#172) | 28 | Sep 20 |
| Post-dry-run: the sheet is the menu both ways | The upload deletes the bets and picks its sheet no longer lists, after showing them | ✅ Code complete Sept 12, 2026 (539 unit tests · lint · tsc · build · `local-db-verify` incl. a 14-check sweep round trip · `dry-run-verify` end to end, pool unchanged at $393). Cut Sept 12, 2026 from Pat. The import had exactly one direction — upsert by sheet key — so the Sept 11 Phase 1 refresh left 19 stale bets live and clearing them needed database access. Now the upload pauses whenever it would delete something, shows what and in which phases, and writes nothing until a human answers; rows carrying wagers are a second, separate answer with the bettors named, because the pool does not change and nobody is notified. PRD §12 **A24** — the first place the app hard-deletes a placement, and the `pre-import` save state is what makes that defensible. **Residue:** the prod migration (#229), the phone-browser pass (#230) and the E2E spec (#231) | 29 | Sep 15 |

---

## Completed Work (Phases 0–3)

Shipped May 7, 2026 (see git history and `docs/superpowers/` for the design specs and plans):

- **Phase 0:** Next.js 15 (App Router) + Tailwind + shadcn/ui skeleton; header; landing page; `/dashboard` and `/bets` routes.
- **Phase 1:** Magic-link login (`/login`, auth callback, signout), session middleware with route protection, `public.users` table + new-user trigger + RLS (`supabase/migrations/20260507000000_users_table.sql`).
- **Phase 2:** `tournaments` + `tournament_participants` tables with RLS and the 2026 seed row (`20260507000001_tournaments.sql`); `/dashboard` showing tournament + registration status.
- **Phase 3:** `bet_categories` / `bets` / `bet_subjects` tables with RLS and the seven-category seed (`20260507000002_bets.sql`); `/bets` menu grouped by round → category with American/fractional/implied odds (`lib/odds.ts`). **⚠️ Superseded in part by ADR 0001** (July 15): the one-odds-per-bet schema, `bet_subjects`, the seven categories, and the computed odds display are all replaced by the bet/pick structure — Sprint 1 reworks this. Auth, tournaments, and the page scaffolding carry forward untouched.

**Phases 0–2 are now verified in production** (Jul 16, 2026) — infra was rebuilt under Andrew's own Vercel + Supabase accounts, taking ownership back from the fork. Production: **https://ozark-open.com** — the canonical domain since Aug 23, 2026; every other alias (`ozark-open-sportsbook.vercel.app`, `www.`) 308s to it and magic links are built from it. Also the auth email sending domain (issue #16). Phase 3 remains ⚠️ rework-required regardless (Sprint 1). See `sprints/sprint-0.md` for the infra facts and the env-var gotcha.

---

## What Is Explicitly Not on the Roadmap

- ~~A custom admin UI beyond `/admin/import`, the read-only `/admin/view` View-sheet replica (Sprint 7), and the `/admin/people` access console (Sprint 20, absorbing the Sprint 16 approval page and the Sprint 10 roster)~~ — **no longer out of scope as of Jul 31, 2026.** The dry run with Pat found two time-critical moments (the chase list, and unlocking `/results`) with no UI at all, plus three admin capabilities he needs. Sprints 23 and 25 add them; Studio remains the fallback for everything not listed there.
- A bet resolution engine (results are computed in the admin's workbook and uploaded — ADR 0001 §3).
- Notifications, email digests, or push notifications (out of scope for v1).
- A mobile app (it's a mobile-responsive web app — no native build).
- Public access, marketing pages, SEO (private app, behind login).
- Multi-tenancy (this is one pool for one group; it does not need to support multiple unrelated leagues).
- Payment *processing* (Venmo and cash, out of band, as today). **Narrowed Sept 2, 2026:** the app now *records* what an admin says has come in (PRD §12 A17) — bookkeeping an admin acts on, never a payment rail, and never an input to pool math.
- Career P/L across tournaments (v2).

If a feature isn't here, default to "no" until someone makes the case for it.

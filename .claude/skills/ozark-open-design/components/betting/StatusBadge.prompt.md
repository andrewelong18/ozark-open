Bet lifecycle badge — open (inviting green + dot), closed (neutral), resolved (quiet indigo). Color is always paired with a label.

```jsx
<StatusBadge status="open" />
<StatusBadge status="closed" />
```

`draft` bets are never shown to participants, so there is no draft variant.

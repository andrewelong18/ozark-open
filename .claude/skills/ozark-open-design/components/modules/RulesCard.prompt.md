Personalized rules reference card — entry fee, max single/self bet, bet counts, exact-total rule. Reference-card energy, not legal fine print.

```jsx
<RulesCard entryFee={40} maxSingle={20} maxSelf={10} minBets={5} maxBets={10} />
```

Values are per-participant (max single = half entry capped at $20; max self = quarter capped at $10).

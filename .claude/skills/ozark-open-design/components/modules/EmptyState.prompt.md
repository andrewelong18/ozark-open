Empty / waiting state with a little personality — "No bets published yet", "Round 2 opens Saturday morning".

```jsx
<EmptyState title="Round 2 opens Saturday" message="Odds drop after Friday's round. Go get a drink." />
```

Optional `glyph` (defaults to ⛳) and `action` button.

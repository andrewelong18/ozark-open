Dashboard stat tile — glanceable label + big tabular number, optional money + caption.

```jsx
<StatCard label="Pool Total" value={520} money feature caption="24 players in" />
<StatCard label="Your Entry" value={40} money />
```

`feature` gives the indigo clubhouse treatment — reserve it for the marquee tile (pool total).
